# ─── Core LangGraph + LangChain ───────────────────────────────────────────────
# LangGraph is the orchestration layer. LangChain provides LLM integrations,
# tool decorators, and message types.
langgraph>=0.2.28
langchain>=0.3.7
langchain-core>=0.3.15
langchain-openai>=0.2.7          # Phase 1 (Step 1). Swapped for langchain-google-genai in Step 2.
langchain-google-genai>=2.0.0          # Gemini LLM (step2) + embeddings (phase2)
google-adk>=1.0.0

# ─── Phase 2 additions (RAG) ──────────────────────────────────────────────────
langchain-text-splitters          # RecursiveCharacterTextSplitter
langchain-community               # PyPDFLoader (document loading)
langchain-chroma                  # ChromaDB LangChain integration
chromadb                          # Vector store — persists to data/chroma_db/
pypdf                             # PDF text extraction (used by PyPDFLoader)

# ─── Serving ─────────────────────────────────────────────────────────────────
# fastapi                        # Uncomment in Phase 3
# uvicorn[standard]              # Uncomment in Phase 3

# ─── Evals ────────────────────────────────────────────────────────────────────
# deepeval                       # Uncomment in Phase 4

# ─── Utilities ───────────────────────────────────────────────────────────────
python-dotenv>=1.0.1             # Load .env API keys
typing-extensions>=4.9.0        # TypedDict compatibility across Python versions