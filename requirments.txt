# ─── Core LangGraph + LangChain ──────────────────────────────────────────────
langgraph>=0.2.28
langchain>=0.3.7
langchain-core>=0.3.15
langchain-community>=0.3.0       # PyPDFLoader used in src/rag/ingest.py
langchain-openai>=0.2.7          # Phase 1 Step 1 only — swapped to google-genai in Step 2

# ─── Google AI ────────────────────────────────────────────────────────────────
langchain-google-genai==2.0.11   # Pinned — 4.x routes embeddings to v1beta, breaks GeminiEmbeddings
google-adk>=1.0.0                # Phase 1 Step 3 — ADK quickstart + pulls in google-genai
# google-genai is installed as a transitive dependency of google-adk.
# Tested working version: 1.75.0. GeminiEmbeddings forces api_version='v1' explicitly
# to bypass the SDK default of v1beta. Do not remove the http_options override.

# ─── RAG (Phase 2 — active) ───────────────────────────────────────────────────
chromadb>=0.5.0                  # Vector store — local dev (Vertex AI Vector Search in prod vision)
langchain-chroma>=0.1.0          # LangChain ↔ ChromaDB integration (Chroma class in retriever.py)
pypdf>=4.0.0                     # PDF loading — required by PyPDFLoader

# ─── Serving (Phase 3 — uncomment when starting Phase 3) ─────────────────────
# fastapi>=0.111.0
# uvicorn[standard]>=0.29.0

# ─── Evals (Phase 4 — uncomment when starting Phase 4) ───────────────────────
# deepeval>=1.0.0

# ─── Utilities ────────────────────────────────────────────────────────────────
python-dotenv>=1.0.1             # Load .env API keys
typing-extensions>=4.9.0        # TypedDict compatibility across Python versions